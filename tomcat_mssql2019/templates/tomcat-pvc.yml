apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fcc-appdata-claim
spec:
  resources:
    requests:
      storage: 10Gi
  storageClassName: azurefile
  accessModes:
    - ReadWriteMany
  volumeName: fcc-appdata-volume
